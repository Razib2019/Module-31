// Map and forEach work as same way but forEach does not return anything where map returns an array
const numbers = [1, 5, 6, 4, 15];
const result = numbers.forEach((n) => console.log(n));
console.log(result);
